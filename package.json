{
  "name": "kriyano",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --experimental-strip-types --experimental-vm-modules --test tests/*.test.mjs",
    "validate": "npm run build && npm test && node scripts/validate-site.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.2.9"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
